functions:
  ping:
    runtime: nodejs
    handler: f.ping
    environment:
      CDS_APP_URL: "{{.cds.app.URL}}"
      CDS_ENV_BIM1: "{{.cds.env.BIM}}"
      CDS_ENV_BIM2: "${env:CDS_ENV_BIM}"
      LANG: "${env:LANG}"

